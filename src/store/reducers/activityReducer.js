const initialState = {
  activities: [],
};

const activityReducer = (state = initialState, action) => {
  // cont {type, payload} = action
  switch (action) {
    default:
      return state;
  }
};

export default activityReducer;
